# 変更履歴

本ファイルの形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン付けは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [Unreleased]

### 追加

- 日時・観測地・ビューを URL パラメータ (`t` / `lat` / `lon` / `city` / `view`) で
  指定できるようにしました。`t` は ISO 8601 で、UTC オフセット付きを推奨します。
  `city` は都市プリセットの名前 (`tokyo` など) で、`lat` / `lon` と併用した場合は
  `lat` / `lon` を使います。省略時と不正値のときは従来どおりの既定動作になります
- 表示中の日時とビューを `history.replaceState` で URL に反映するようにしました。
  アドレスバーをコピーすればそのまま共有できます
- 設定パネルに「この空の URL を作る」を追加しました。共有 URL の表示とコピーができます。
  観測地が都市プリセット以外の座標 (現在地の取得・手入力) の場合は、緯度経度が
  その場所を伝えることになる旨の注意書きを表示し、含めるかどうかを選べます
  (既定では含めません)
- 緯度経度はアドレスバーには出しません。注意書きと同意のある共有ダイアログを
  通らずにアドレスバーをコピーするだけで座標が渡ってしまうのを防ぐためです。
  観測地が都市プリセットのときだけ、座標ではなく名前 (`city=tokyo`) で反映します。
  URL パラメータで座標を渡して開いた場合も、観測地には反映しますがアドレスバーには
  残しません

### 変更

- 昼間に開いた場合、基準日時をその日の 20:00 に設定するようにしました。
  起動時の太陽高度が −6° より高いときのみ適用し、夜間に開いた場合は現在時刻のままです。
  現在時刻に戻すには設定パネルの「現在時刻に戻す」を使用してください

### 修正

- 上記の 20:00 を、ブラウザのローカルタイムゾーンではなく観測地の時刻
  (経度から求めたオフセット) で解釈するようにしました。実行環境のタイムゾーンと
  観測地がずれている場合 (UTC のマシンから既定の東京を見る場合など) に、
  昼間対策の分岐が昼の空を選んでしまう不具合を修正しています

- 3D ビュー表示中に画面サイズが変わると (端末の回転、モバイルブラウザの
  URL バーの開閉など)、2D に戻したときに何も描画されなくなる不具合を修正しました。
  非表示のキャンバスは大きさが 0 になるため、リサイズ処理がその値を反映して
  キャンバスを 0×0 に潰していました。同じ原因で 3D カメラの aspect が NaN になる
  経路もあわせて塞いでいます

## [1.0.0] - 2026-08-11

### 追加

- 2D 星座早見ビュー (天頂中心の正距方位図法、北を上・東を左)
- 3D プラネタリウムビュー (three.js。ドラッグで視線移動、ピンチで画角)
- 日時入力と観測地入力 (緯度経度、10 都市のプリセット、Geolocation API)
- 基準日時 ±24 時間のタイムスライダーと再生 (×1 〜 ×86400)
- 恒星 5,070 個 (6.0 等まで)、88 星座の星座線、星座名 (日本語)、天の川の描画
- 太陽・月・惑星 7 個の位置計算と描画。月は満ち欠けを描画
- 人工衛星 3 基 (ISS・CSS・ハッブル) の SGP4 による軌跡表示と地球影の判定
- 大気表現 (太陽高度に応じた空の色、薄明のグラデーション、星の減光)
- 天体タップによる名称・等級・高度方位の表示
- 天体検索 (恒星の和名エイリアス、星座の和名と略符、惑星、衛星)。
  2D はマーカー、3D は視線移動と画面外方向ガイド
- 方位センサー連携 (3D)。iOS の許可要求、Android の絶対方位イベントに対応し、
  ドラッグによる方位オフセット補正が可能
- 表示要素ごとのオンオフ切り替え
- 単一 HTML への埋め込みビルド、生成物の静的検査、Playwright による動作確認

[Unreleased]: https://github.com/sabas0ba/hoshizora/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/sabas0ba/hoshizora/releases/tag/v1.0.0
